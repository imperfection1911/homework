---
 - name: configure logging
   template:
     src: logging.conf.j2
     dest: /etc/rsyslog.d/logging.conf
 - name: configure rsyslog server
   template:
     src: rsyslog.conf.j2
     dest: /etc/rsyslog.conf
   notify:
     - restart_rsyslog

